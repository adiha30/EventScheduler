package com.adiha.EventScheduler.models.enums;

/**
 * This enum represents the roles that a user can have in the system.
 * The roles are:
 * - USER: A regular user of the system.
 * - ADMIN: An administrator with elevated privileges.
 */
public enum Role {
    USER,
    ADMIN
}